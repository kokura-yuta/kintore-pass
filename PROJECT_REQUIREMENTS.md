# MUSCLE PATH 設計方針

このファイルには、現在のコード説明ではなく、今後の実装で守る要件を記録します。

## AIチャットの最重要要件：長期記憶

AIチャットは、その場の1回だけ回答する機能ではなく、利用者について継続的に記憶し、過去の情報を踏まえて回答する機能にする。

## AIチャットと他機能の関係

AIチャットは独立した機能ではなく、アプリ内の他機能で登録したデータを理解して会話する中心機能とする。

```text
理想の体を選ぶ ──────────┐
身体分析を行う ──────────┤
トレーニングを記録する ──┤
週間メニューを作る ──────┤→ 共通の利用者データ → AIチャット
ダッシュボードで進捗管理 ─┘
```

### 具体例

利用者が「理想の体」機能で細マッチョを選択した場合、次のような共通データとして保存する。

```jsx
goalBodyType: "細マッチョ"
```

その後、AIチャットで「今日何をすればいい？」と質問された場合、AIは目標をもう一度聞かず、細マッチョを目指していることを前提に回答する。

```text
利用者：今日何をすればいい？

AI：細マッチョを目標にしているので、今日は筋量を維持しながら
    消費量も確保できる上半身メニューを行いましょう。
```

身体分析、トレーニング記録、週間メニューについても同様に、AIチャットが必要な情報を取得できるようにする。

### データ管理の方針

- 各画面とAIチャットで別々のコピーを持たず、共通の利用者データを参照する
- 画面で変更した情報は保存し、AIチャットでも同じ最新情報を使用する
- AIが会話だけから推測した情報と、利用者が画面で確定した情報を区別する
- 目標などの重要情報をAIが勝手に上書きせず、変更時は利用者へ確認する
- AIへは毎回、今回の質問に必要な共通データだけを渡す

### 将来のToolとの対応

AIが他機能のデータを扱うときは、用途ごとにToolを分ける。

```text
get_body_goal
→ 選択中の理想体型を取得する

get_body_analysis
→ 最新の身体分析結果を取得する

get_training_history
→ 過去のトレーニング記録を取得する

get_weekly_menu
→ 現在の週間メニューを取得する

save_training_log
→ 会話から新しいトレーニング記録を保存する
```

AIチャットはこれらのToolを通して他機能と連携し、画面と同じ保存データを参照する。

### 記憶する情報

- 選択した理想の体型
- 利用者が登録した参考画像や身体分析の結果
- 過去のトレーニング記録
- 作成済みの週間メニュー
- 過去に相談した内容
- AIが以前伝えたアドバイス
- 重量、回数、体重、達成率などの変化
- 痛みや苦手種目など、今後の提案で注意すべき情報

### 記憶を回答へ使う例

```text
以前：胸に効かないと相談した
その後：フォーム改善を試した記録がある
今回：重量が伸びないと相談した

回答：以前のフォーム改善後の記録も確認しながら、
      重量設定や回復状況を含めて提案する
```

### 実装上の方針

- Reactの`useState`だけを長期記憶には使用しない
- ブラウザを閉じても消えない保存先を用意する
- 会話全文だけでなく、重要情報を整理したプロフィール・要約も保存する
- トレーニング記録などの事実と、AIの会話要約を分けて管理する
- AIへ毎回すべての会話を渡さず、今回の質問に必要な記憶を選んで渡す
- 利用者が記憶内容を確認・修正・削除できるようにする
- 身体画像や健康に関する情報は、通常の会話より慎重に扱う
- APIキーや秘密情報をReactの画面側へ直接書かない

### 開発する順番

1. チャット入力欄を作る
2. 画面内で会話履歴を管理する
3. 仮のAI回答でチャット画面の動きを完成させる
4. 会話と利用者情報の保存方法を決める
5. サーバー側からAIへ接続する
6. 必要な記憶を検索して回答へ含める
7. 記憶の確認・修正・削除画面を作る

長期記憶を重視するが、最初からすべてを同時に実装せず、入力・表示・保存・AI接続の順で理解しながら進める。

## AIチャット10段階の進み方

現在は、トップページから`/chat`へ移動し、専用ページで仮の会話ができるところまで実装済み。

1. 利用者・AIの吹き出し：完了
2. 仮のAI回答：完了
3. 送信中・エラー表示：完了
4. 専用チャット画面の完成：完了
5. バックエンドのチャット受付処理を作る：完了
6. AI用プロンプトを作る：完了
7. フロントエンドとバックエンドを接続する：完了
8. 他機能の共通データをAIが参照する仕組みを作る
9. 長期記憶の保存・取得・修正・削除方針を実装する
10. Toolを追加して、体型目標やトレーニング履歴を取得できるようにする

次の作業は「8. 他機能の共通データをAIが参照する仕組みを作る」です。
