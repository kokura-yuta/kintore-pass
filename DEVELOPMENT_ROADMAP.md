# MUSCLE PATH 30日完成ロードマップ

## このロードマップの完成基準

30日後に、次の機能がつながった「自分で使えるMVP」を完成させます。

- 理想の体型を選び、AIが目標として理解できる
- 身体写真をアップロードし、AIによる参考分析を表示できる
- 種目・重量・回数・セットを記録できる
- 自然文からトレーニング記録を作成できる
- 条件から週間トレーニングメニューを作成できる
- ダッシュボードで今日の予定と進捗を確認できる
- AIチャットが目標・分析・記録・メニューを参照できる
- 会話と重要情報を長期保存できる
- 利用者が記憶内容を確認・修正・削除できる
- エラー処理、基本テスト、公開前確認が終わっている

身体写真からの評価は医療診断ではなく、トレーニング方針を考えるための参考情報として扱います。独自の画像認識モデルが必要になるまではJavaScriptと外部AI APIで進め、Pythonは必要性が明確になった時点で追加します。

## 毎日の時間配分

### 実装：2時間

```text
0:00〜0:15  今日変更する場所とデータの流れを確認
0:15〜1:35  コードを書く
1:35〜1:50  ブラウザで動作確認
1:50〜2:00  CODE_GUIDE.mdと進捗を更新
```

### コード理解：1時間

```text
0:00〜0:20  今日書いたコードを上から説明する
0:20〜0:40  基本の型と単語帳を復習する
0:40〜0:55  コードを見ずに小さく書き直す
0:55〜1:00  分からない用語を翌日の確認項目へ残す
```

## 現在地

- AIチャット10段階の1〜7まで完了
- `/chat`から`POST /api/chat`へ質問を送信できる
- バックエンドの`reply`をAI吹き出しへ表示できる
- システムプロンプトの土台を作成済み
- 現在は8番「他機能の共通データをAIが参照する仕組み」の途中
- 次のコード変更は、体型ボタンの`onClick`を`handleBodyTypeSelect`へ接続する作業

# 1週目：共通データと本物のAIチャット

| 日 | 実装2時間 | 理解1時間 | その日の完了条件 |
| --- | --- | --- | --- |
| 1日目 | 体型ボタンと`handleBodyTypeSelect`を接続し、選択した体型を`localStorage`へ保存する | 関数、引数、`onClick`、アロー関数、`localStorage.setItem()` | 体型を選ぶと`goalBodyType`として保存される |
| 2日目 | チャットページで`goalBodyType`を読み、質問と一緒に`/api/chat`へ送る | `localStorage.getItem()`、オブジェクト、JSONの入れ子 | バックエンドが質問と目標体型を受け取れる |
| 3日目 | バックエンドの仮レスポンスへ目標体型を反映し、共通データ接続を確認する | `body.userData.goalBodyType`、プロパティ参照、データの流れ | 「細マッチョを目標にしています」のような仮回答が返る |
| 4日目 | OpenAIライブラリの導入準備、APIキーの安全な設定、環境変数ファイルの準備 | npmパッケージ、環境変数、サーバーとクライアントの違い | APIキーがフロントエンドへ露出しない構成になる |
| 5日目 | `route.js`からOpenAI APIへ質問とシステムプロンプトを送る | SDK、インスタンス、Responses API、`await`、APIレスポンス | 固定文ではなくAIが作った回答をバックエンドで取得できる |
| 6日目 | AIの回答をチャット画面へ表示し、通信失敗・API失敗を分けて扱う | HTTPステータス、`response.ok`、`throw new Error()`、`try/catch` | 本物のAIとチャットでき、失敗時にエラーが表示される |
| 7日目 | 1週目のコード整理、コメント、コードガイド、動作テスト | 1週目の単語帳を見ずに説明・再現する | AIチャット10段階の8番までの基礎が安定して動く |

# 2週目：データベース、トレーニング記録、長期記憶

| 日 | 実装2時間 | 理解1時間 | その日の完了条件 |
| --- | --- | --- | --- |
| 8日目 | 利用者プロフィール、目標、記録、会話、記憶のデータ構造を設計する | テーブル、行、列、主キー、外部キー | 保存するデータと各テーブルの役割が決まる |
| 9日目 | データベース接続と最初のプロフィール・目標保存APIを作る | DB接続、CRUD、`INSERT`、`SELECT` | 目標体型をDBへ保存・取得できる |
| 10日目 | `localStorage`の体型目標をDB保存へ置き換える | 一時保存と永続保存、非同期保存、API設計 | 再読み込み後も選択中の体型が復元される |
| 11日目 | トレーニング記録フォームのStateと入力欄を作る | 数値入力、制御コンポーネント、フォーム設計 | 種目・重量・回数・セットを入力できる |
| 12日目 | トレーニング記録の保存・一覧取得APIを作る | POSTとGET、バリデーション、配列の表示 | 記録を保存し、画面へ一覧表示できる |
| 13日目 | 自然文のトレーニング記録をAIで構造化する | 構造化出力、JSON Schema、入力検証 | 「ベンチ50kgを10回3セット」を記録候補へ変換できる |
| 14日目 | 会話履歴を保存し、チャットを開いたときに取得する | 会話ID、`user`と`assistant`、時系列、取得件数 | 再読み込み後も最近の会話が表示される |

# 3週目：Tool、記憶、身体分析

| 日 | 実装2時間 | 理解1時間 | その日の完了条件 |
| --- | --- | --- | --- |
| 15日目 | `get_body_goal`と`get_training_history`のTool設計・実装 | Toolの名前、説明、引数、返り値、実行の流れ | AIが必要に応じて目標と記録を取得できる |
| 16日目 | AIチャットへTool実行ループを接続する | Tool call、結果の返却、停止条件、無限ループ防止 | 「今日何する？」へ目標と履歴を踏まえて回答する |
| 17日目 | 会話から長期記憶候補を抽出し、事実とAI要約を分けて保存する | 短期履歴と長期記憶、要約、信頼できる事実 | 痛み・苦手種目・目標などを記憶候補として保存できる |
| 18日目 | 記憶内容の確認・修正・削除画面を作る | CRUD、確認操作、重要情報の上書き防止 | 利用者がAIの記憶を管理できる |
| 19日目 | 正面・横・背面の身体写真アップロードUIを作る | ファイル入力、プレビュー、ファイル種別・容量検証 | 3方向の写真を選択し、送信前に確認できる |
| 20日目 | 身体写真の安全なアップロード先と削除処理を作る | ファイル保存、アクセス制御、保存期限、削除 | 写真を許可された利用者だけが扱える |
| 21日目 | 画像対応AIへ写真を送り、部位別評価を構造化して返す | 画像入力、構造化結果、分析の限界、安全な表現 | 肩・胸・背中・腕などの参考評価がJSONで返る |

# 4週目：身体分析UI、AIメニュー、ダッシュボード

| 日 | 実装2時間 | 理解1時間 | その日の完了条件 |
| --- | --- | --- | --- |
| 22日目 | 身体分析結果と理想との差を画面へ表示・保存する | スコア表示、条件レンダリング、比較データ | 現在の状態と優先部位を確認できる |
| 23日目 | AIメニュー用の週回数・時間・場所フォームを作る | ラジオ、セレクト、数値State、フォーム検証 | メニュー作成条件を入力できる |
| 24日目 | 目標・分析・履歴を使って週間メニューをAI生成する | プロンプトへのコンテキスト、構造化出力 | 曜日別の種目・回数・セットが生成される |
| 25日目 | 週間メニューの保存、表示、実施済み切り替えを作る | ネストした配列、更新API、チェック状態 | 作成したメニューを毎日使って記録できる |
| 26日目 | ダッシュボードへ今日のメニューと今週の達成率を表示する | 集計、割合計算、日付処理 | 今日やることと今週の進み具合が分かる |
| 27日目 | 優先部位と最近のAIアドバイスをダッシュボードへ追加する | 複数APIのデータ統合、表示優先順位 | 4つのダッシュボード項目がそろう |

# 最終3日：統合、品質確認、公開準備

| 日 | 実装2時間 | 理解1時間 | その日の完了条件 |
| --- | --- | --- | --- |
| 28日目 | AIチャットが目標・分析・記録・メニューをすべてToolで参照できるよう統合する | Tool選択、必要なデータだけ渡す考え方 | 代表的な4質問へアプリデータを踏まえて答える |
| 29日目 | スマホ表示、キーボード操作、入力エラー、読み込み状態、データなし状態を確認する | アクセシビリティ、レスポンシブ、例外処理 | 主要画面で操作不能・表示崩れ・致命的エラーがない |
| 30日目 | 全機能の通しテスト、コードガイド最終更新、秘密情報確認、公開準備 | アプリ全体のデータフローを自分の言葉で説明する | MVP完成。残課題と次バージョンの内容が整理される |

# 毎週の確認質問

各週の7日目に、コードを見ずに次を説明できるか確認します。

## 1週目

- フロントエンドとバックエンドの違いは何か
- JavaScriptオブジェクトとJSONの違いは何か
- `fetch()`、`request.json()`、`Response.json()`の順番はどうなるか
- APIキーをReact側へ書いてはいけない理由は何か

## 2週目

- State、localStorage、データベースの違いは何か
- POST、GET、保存、取得はどのファイルが担当するか
- トレーニング記録1件はどんなオブジェクトになるか
- 会話履歴を配列としてどう表示するか

## 3週目

- 長期記憶と会話履歴の違いは何か
- Toolはなぜ必要か
- AIが事実と推測を混ぜないために何が必要か
- 身体写真を安全に扱うために何が必要か

## 4週目

- 理想の体型から今日のメニューまで、データがどう流れるか
- AIメニューの入力と出力はどんなJSONか
- ダッシュボードの達成率をどう計算するか
- AIチャットが各機能のデータをどう取得するか

# 遅れた場合の優先順位

時間が足りない場合は、次の順番を守ります。

1. データを安全に保存できる
2. AIチャットが目標とトレーニング記録を参照できる
3. トレーニング記録と週間メニューが使える
4. ダッシュボードで今日やることを確認できる
5. 身体写真分析を追加する
6. 見た目の細かい改善を行う

身体写真分析は安全性と精度確認が必要なため、間に合わない場合は無理に公開せず、写真アップロードなしのMVPを先に完成させます。

# 完成後に追加する候補

- Pythonによる独自の姿勢・関節位置・輪郭分析
- 複数端末・複数利用者向けの認証強化
- 音声入力
- 通知とリマインダー
- 体重・体脂肪・使用重量のグラフ
- メニューの自動調整
- 写真比較のタイムライン
- 本格的なテスト自動化と監視
