# 筋トレPAS バックエンドロードマップ

最終更新日：2026-09-12

このファイルは、バックエンドの完成状況・残作業・実装順を記録する進捗表です。

## 進め方

- コードを書くのは基本的にユーザー本人。
- Codexは最初に「何を・どこへ・何のために書くか」を説明する。
- ファイル作成、明確に依頼された修正、競合解消はCodexが担当する。
- 完了するたびに、このファイルと`CODE_GUIDE.md`を更新する。
- セキュリティとデータ削除は、動作確認後に完了とする。

## 現在地

### 2026年9月13日の食事・AI・アカウント削除の最終確認

- [x] 認証付きE2E 29項目で、初回登録・体重・食事・トレーニング・AIメニュー・ユーザー分離・各削除を再確認
- [x] 合成画像3枚で`TypeScript → Python → OpenAI → Neon → 履歴GET`の身体分析を実通信確認
- [x] 身体分析専用の再実行可能な`test:body-analysis-live`を追加
- [x] 長いOpenAI処理後もテストを続けられるよう、Clerkの短命JWTを次の通信前に更新
- [x] Expo Webの主要画面を確認し、390×844で横方向の表示崩れがないことを確認
- [x] 一時Clerkユーザーで食事をNeonへ保存し、別GET通信でも同じ内容が残ることを確認
- [x] AIチャットが食事Toolを実行し、固有の食事名・777kcal・たんぱく質55gを回答へ反映することを確認
- [x] AIメニューが最近7日間の食事を含む文脈で生成され、メニューと種目が保存されることを確認
- [x] 食事DELETE後に一覧とNeonの両方から消えることを確認
- [x] 古いセッションでアカウント削除APIが403を返し、Clerk再本人確認を要求することを確認
- [x] テスト用Neonユーザーの関連データがcascadeで全削除され、Clerkテストアカウントも消えることを確認
- [x] マイページへ`DELETE`確認入力、最終確認、Clerk再確認、処理中・エラー表示を追加
- [x] Sitesバージョン7を公開し、TypeScript API・Neon・RenderがHTTP 200であることを確認
- [x] 全テスト合格：入力検査18件、セキュリティ8件、Python 12件、mobile通信19件、型・Lint・Build・Neon・公開API

### 2026年9月12日の食事管理対応

- [x] `food_records`テーブルと入力範囲・食事区分・本人検索用インデックスを追加
- [x] 食事記録のPOST・GET・PATCH・DELETE APIを追加
- [x] Expoの食事画面を仮保存からClerk認証付きAPIへ接続
- [x] 取得中・保存中・削除中・エラー表示と二重送信防止を追加
- [x] AIメニューへ最近7日間の食事記録を渡す
- [x] AIチャットへ最近7日間の食事を取得するToolを追加
- [x] 開発用・公開用Neonへ`food_records`を反映
- [x] Clerk実セッション2人分で保存・取得・更新・削除・他人操作拒否・cascade削除を確認
- [x] TypeScript、Expo、入力検査、通信、Drizzleマイグレーション検査に合格

食事管理のコード・Neon保存・公開APIへの反映は完成しました。さらに一時Clerkユーザーを使い、保存、別通信での再取得、削除、Neonからの消去、AIチャットへの実反映、AIメニュー生成を実通信で確認しました。Sitesバージョン6の公開ヘルスチェックと未ログイン保護も合格しています。

- [x] 2026-09-13に全自動テストを実行し、API安全18件、秘密情報・安全検査8件、Python 12件、mobile通信19件、型・Lint・Build・Neon実DB・公開APIがすべて合格
- [x] 公開TypeScript API、Neon、RenderのヘルスチェックがHTTP 200

### 2026年9月11日の公開・監視確認

- [x] Cloudflare Sitesバージョン5へ最新バックエンドを公開
- [x] 公開`GET /api/health`がTypeScript APIだけでなくNeonへ`SELECT 1`を実行することを確認
- [x] 公開環境で`dependencies.database: "ok"`とHTTP 200を確認
- [x] RenderのPython分析APIがHTTP 200を返すことを確認
- [x] 公開APIの未ログイン保護テストに合格
- [x] GitHub Actionsの定期監視が`active`で、直近の定期実行が成功していることを確認
- [x] 単体・セキュリティ・Python・mobile・型・Lint・Build・Neon実DB・公開APIの全テストに合格

バックエンドのコード作成と自動テストは完了しています。残る大きな作業は、OpenAI管理画面の予算アラート確認とApp Store公開準備です。Clerk本番キーへの切り替え、iPhone実機確認、利用規約・プライバシー表示、課金はApp Store公開準備に含めます。

- バックエンドMVP進捗の目安：約99%
- 現在の次作業：OpenAI予算アラートの管理画面確認。その後はApp Store公開準備へ進む
- ログ・監視：APIリクエストID、安全なエラーログ、OpenAIトークン使用量ログを実装しローカル本番通信で確認済み
- 保留：Neonデータ転送量はHTTP 402で停止中。Neon CLI表示の次回リセットは2026-10-01 00:00 UTC
- ホームAPI：`GET /api/home`のコード・フロント接続・Neon実通信を確認済み
- 開発DB接続：`musclepas-development`への切り替えと認証付き`bootstrap` HTTP 201を確認
- 通信量対策：トレーニング履歴・ホーム・AIメニュー履歴の無限再取得を修正。1タブの実通信で連続呼び出しが止まることを確認
- アカウント削除API：Expo再確認UI、strict再確認要求、Neon cascade、Clerkテストアカウント削除を確認済み
- トレーニング記録：PATCH・DELETE・履歴絞り込み・ページ分けのコード完成
- 開発中の設定値：`AI_CHAT_DAILY_LIMIT=100`
- AIメニューの設定値：`AI_MENU_DAILY_LIMIT=3`

## 完成済み

- [x] Clerkのセッショントークン検証
- [x] ClerkユーザーとNeonユーザーの紐付け
- [x] bootstrap APIと初回設定判定
- [x] 理想体型の保存・取得
- [x] 身体プロフィールの保存・取得
- [x] 初回設定完了API
- [x] トレーニング記録の保存・履歴取得
- [x] Python FastAPIとOpenAIによる身体画像分析
- [x] 画像の形式・容量・破損確認
- [x] 身体分析結果と部位別評価のNeon保存・履歴取得
- [x] 身体分析の日本時間1日1回制限
- [x] Python分析APIのRender公開
- [x] AIメニュー生成・Neon保存・最新取得
- [x] AIチャットの質問送信・回答生成
- [x] チャットルームとメッセージのNeon保存
- [x] チャット履歴の取得・画面復元・削除
- [x] AIチャットへ直近20件の会話を渡す
- [x] プロフィール取得Tool
- [x] 最新身体分析取得Tool
- [x] 最近のトレーニング記録取得Tool
- [x] 最新AIメニュー取得Tool
- [x] 最近7日間の食事記録取得Tool
- [x] 食事記録の保存・日付別取得・更新・削除
- [x] ホーム用`GET /api/home`の作成とTypeScript型検査

## 最優先：MVP公開前に必要

### 1. AI利用回数と料金の制限

- [x] `.env.local`へ開発用の`AI_CHAT_DAILY_LIMIT=100`を設定
- [x] 日本時間の1日の範囲を計算
- [x] 本人が今日送った質問数をNeonから数える
- [x] 設定した上限以上ならHTTP 429を返す
- [x] `Retry-After`と次回利用可能日時を返す
- [x] 残り利用回数をレスポンスへ含める
- [x] AIメニュー生成を日本時間で1日3回に制限
- [x] AIチャットとAIメニューの5秒以内の連続送信を防ぐ
- [ ] 同じ質問やメニューの二重送信を防ぐ
  - [x] `ai_request_guards`のスキーマと安全なマイグレーションを作成
  - [x] スマホでUUIDを生成してチャット・メニューAPIへ送信
  - [x] バックエンドで重複IDをHTTP 409として拒否
  - [x] 失敗時に受付記録を削除して再試行を許可
  - [ ] Neonへテーブルを反映（データ転送量上限超過で保留）
  - [ ] 実通信で同じIDの2件目が拒否されることを確認
- [ ] OpenAI利用量と料金を確認できるようにする
  - [x] AIチャット・AIメニュー・身体分析で、本文を含めず使用トークン数だけを構造化ログへ記録
  - [ ] 公開先のログ保存先とOpenAI Platform側の予算アラートを本番公開時に設定

### 2. OpenAI・Python通信の停止対策

- [x] TypeScriptからPythonへの通信を120秒で中止しHTTP 504を返す
- [x] AIチャットとAIメニューのOpenAI通信を1回60秒・最大1回再試行に制限
- [x] PythonからOpenAIへの通信を1回50秒で中止
- [x] SDKが一時的と判断する通信エラーだけを最大1回再試行
- [x] Render停止・起動中は分かりやすいHTTP 503を返す
- [x] 画像を1枚8MB・3枚24MB・通信全体26MBへ統一

ローカル検査では、PythonのOpenAIタイムアウトをHTTP 504、接続障害をHTTP 503として返す模擬テストに成功しました。実際のRender環境へ新しいコードを反映した後、公開URLで最終確認します。

### 3. TypeScriptバックエンドの開発用公開

- [x] 公開先をCloudflareに決定（Python画像分析は既存Renderを継続）
- [x] Cloudflare・Worker型エラーをすべて修正
- [x] 公開先へNeon・Clerk・OpenAI・Python URLの環境変数を設定
  - [x] Neon・Clerk・Python URL・利用上限をSitesへ登録
  - [x] `OPENAI_API_KEY`を値を公開せず秘密設定として登録
- [x] `.env.local`を開発用、Sites環境変数を本番用として分離
- [x] 公開API URLをフロント担当へ共有できる状態にする
  - [x] `mobile/.env.local`の接続先を公開APIへ変更
  - [x] `mobile/.env.example`へ公開API URLを記載
- [ ] Expoから全APIへ接続できるか確認
  - [x] Expo WebでCORSエラーを再現して原因を特定
  - [x] 許可した開発URLだけを通すCORS処理を追加
  - [x] CORS修正版をSitesバージョン2として再公開
  - [x] ExpoからCloudflareへ到達し、Clerk認証を通過することを確認
  - [x] Sitesバージョン3へ最新バックエンドを公開
  - [x] 開発用Neonで認証付き`bootstrap`・ホーム・AIメニュー・チャット・記録履歴・身体分析履歴の取得を確認
  - [x] 開発用Neonの13テーブルとマイグレーション履歴を確認
  - [x] ログイン済みExpo Webからトレーニング記録・AIメニュー・AIチャットの保存と再取得を確認
  - [ ] 更新・削除APIのログイン済み画面からの実通信を確認
  - [x] 体重履歴画面を仮データから実APIへ接続
- [x] 公開環境のヘルスチェックを追加
  - [x] `GET /api/health`を作成
  - [x] ローカルの本番起動方式で正常応答を確認
  - [x] Cloudflare公開URLでHTTP 200・`production`を確認
  - [x] 未ログインで本人データAPIのGET・POST・PATCH・DELETE全26通信がHTTP 401になることを確認

### 4. 体重履歴API

- [x] `weight_records`テーブルを設計
- [x] ユーザーID・日付・体重を保存する列を定義
- [x] 同じユーザー・同じ日付の重複をDB制約で防ぐ
- [x] 体重記録POST・GET・DELETE・更新API
  - [x] POST・GET・PATCH・DELETEのコード作成
  - [x] TypeScript型検査
  - [x] 開発用Neonで保存・取得・更新・削除と同日重複拒否を自動テスト
  - [ ] ログイン済み画面からAPI経由の保存・取得・更新・削除を確認
- [x] GETでグラフ用の古い順データと変化概要を返す
- [x] AIチャット用の体重変化Toolを追加
- [x] DrizzleマイグレーションをNeonへ適用
  - [x] `0006_many_namora.sql`を生成して内容確認
  - [x] 開発用Neonへ適用済みであることを実DBテストで確認
  - [ ] 体重APIとAI Toolの実通信テスト

### 5. 現在のホーム画面と実データの接続

現在の`mobile/src/app/home.tsx`に存在する表示だけを初期対象にする。

#### バックエンド担当

- [x] 既存APIの個別呼び出しではなく`GET /api/home`へまとめる方針を確定
- [x] Clerkトークンでログイン中の本人を確認
- [x] 本人の目標体型をNeonから取得
- [x] 本人の最新AIメニューを生成日時の新しい順で1件取得
- [x] 最新AIメニューに所属する種目を表示順で取得
- [x] 目標体型・おすすめ部位・理由・種目・推奨時間・調子・AIメッセージをJSONで返す
- [x] AIメニュー未生成時に`menu: null`と`aiMessage: null`を返す
- [x] 未ログイン時401・ユーザー未登録時404・サーバーエラー時500を返す
- [x] `npx tsc --noEmit`で型エラーがないことを確認
- [x] 開発用Neonで認証付き`GET /api/home`の実通信を確認

#### フロント担当へ共有する作業

- [x] ホームAPIの返却形式をフロント担当へ共有・確定
- [x] 現在の目標体型を実データで表示
- [x] 今日のAIメニュー・おすすめ部位・理由・種目を実データで表示
- [x] AIメッセージ・推奨時間・コンディションを実データで表示
- [x] ホーム起動時の読み込み中・エラー・データなし表示
- [x] 通常動作を実データ取得へ切り替え、`homePreview`は開発用バイパス時だけ使用

バックエンドが返す基本形式は次のとおり。

```text
メニュー生成済み
→ goalBodyType + menu + aiMessage

メニュー未生成
→ goalBodyType + menu: null + aiMessage: null
```

#### ホーム・ダッシュボード拡張（フロントUI追加後）

次の項目は現在のホーム画面に表示欄がないため、フロント担当がUIを追加した後に実装する。

- [ ] 今週のトレーニング回数と達成率
- [ ] 最近鍛えていない部位
- [ ] 最新身体分析の優先部位
- [ ] 最近のAIアドバイス専用表示

### 6. アカウントと全データ削除

- [x] 本人の再確認
  - [x] バックエンドでClerkの`strict`再確認を要求
  - [x] 誤操作防止として確認文字`DELETE`を要求
  - [x] Expo側で`useReverification()`を使いClerk再確認画面を表示
- [x] ClerkアカウントとNeonの`users`データを削除
  - [x] 認証トークンから取得した本人のClerkユーザーIDだけを使用
  - [x] Neonの本人`users`行を削除するコードを作成
  - [x] Clerkの本人アカウントを削除するコードを作成
  - [x] 一時テストユーザーでNeonとClerk双方から消えることを実通信確認
- [x] cascadeで関連データが削除されるか確認
  - [x] `schema.ts`上で全関連テーブルの`onDelete: "cascade"`を確認
  - [x] 実Neonでプロフィール・食事・AIメニュー・チャットを含む関連データの消去を確認
- [x] 身体写真は長期保存していないため、現時点で画像ファイルの削除は不要
- [x] Neon削除後にClerkだけ失敗した場合、HTTP 502と`retryable: true`を返して再実行可能にする
- [x] ClerkとNeonをまたぐ途中失敗はNeonを先に削除し、Clerk失敗時は再実行可能にする方針を確定
- [x] 削除対象IDをフロント入力から受け取らず、認証済み本人IDだけを使うことを自動検査

`npm run test:food-ai-account-live`で18項目を確認済みです。自動作成した古いセッションでは削除APIが403を返すこと、再確認を要求するClerkエラー形式、テスト用Neon関連データのcascade削除、テスト用Clerkアカウントの削除まで確認しています。実利用画面ではClerkの確認コード入力後に同じAPIが自動再送されます。

## MVP品質を上げる作業

### 7. トレーニング記録の編集・削除

- [x] トレーニング記録PATCH・DELETE API
  - [x] DELETEのコード作成とTypeScript型検査
  - [x] PATCHのログイン確認・JSON取得・UUID検査
  - [x] PATCHの共通入力検査
  - [x] PATCHのNeon更新処理
- [x] 本人の記録だけを変更可能にする
  - [x] DELETEは記録IDと本人`userId`の両方を削除条件に使用
  - [x] PATCHも記録IDと本人`userId`の両方を更新条件に使用
- [x] 種目・セットをまとめて安全に更新
  - [x] `neon-http`では対話型`db.transaction()`が使えないことを確認
  - [x] `db.batch()`で親更新・旧種目削除・新種目・新セット保存を一括実行
- [x] 履歴の日付・部位絞り込みとページ分け
  - [x] `page`・`limit`・`dateFrom`・`dateTo`・`bodyPart`を検査
  - [x] 既存フロントと互換性のある`records`と追加の`pagination`を返す
  - [ ] Neon上限回復後に実データで絞り込み・ページ移動を確認
- [x] カレンダーの編集・削除UIをPATCH・DELETE APIへ接続
  - [x] 保存済み種目・セット・時間・調子・メモを編集画面へ復元
  - [x] 更新・削除後に履歴Contextへ即時反映
  - [x] 削除前の確認と二重削除防止を追加
  - [x] PATCH・DELETEの通信JSONを自動テスト
  - [x] 開発用一時データで保存・カレンダー表示・編集復元・更新後の画面遷移を確認
  - [ ] ログイン済み実データでPATCH・DELETEを最終確認

### 8. AIメニュー履歴

- [x] 過去のAIメニュー一覧・詳細・削除
  - [x] 本人の履歴だけを新しい順・ページ単位で取得
  - [x] 本人のメニュー詳細と所属種目を取得
  - [x] 本人のメニューだけを削除し、所属種目はcascadeで削除
  - [ ] Neon上限回復後に一覧・詳細・削除を実データで確認
- [x] メニューを実施したか保存
  - [x] 本人のメニューと本人のトレーニング記録だけを結び付ける
  - [x] 同じ内容の二重送信は成功済みとして返し、別記録への上書きは拒否
  - [ ] Neonへ新しい列を反映し、実施状態を実データで確認
- [x] 再生成回数制限
  - [x] AIメニュー生成を1日3回までに制限
- [x] 同じメニューが続きすぎない調整
  - [x] Neonから本人の直近3メニューと種目を取得
  - [x] 直近メニューをOpenAI入力へ追加
  - [x] 基本種目の継続性を残しながら完全重複を避けるルールを追加
  - [ ] Neon上限回復後に連続生成で実結果を確認

### 9. データ保存の一貫性

2026年8月31日に、保存処理・使用中のDrizzle実装・DB制約・マイグレーション履歴を点検しました。

#### トランザクション点検

- [x] トレーニング本体・種目・セットをトランザクション化
  - [x] 新規保存`POST`が本体・種目・セットを別々に保存していることを確認
  - [x] 本体・全種目・全セットのIDを保存前に生成する
  - [x] 全INSERTを1つの`db.batch()`へまとめる
  - [x] 開発用Neonで子の保存を意図的に失敗させ、`training_sessions`が残らないことを確認
- [x] 身体分析本体・部位別結果をトランザクション化
  - [x] 分析本体を保存した後、部位別結果を別処理で保存していることを確認
  - [x] 分析本体と全部位のIDを保存前に生成
  - [x] 分析本体と全部位のINSERTを1つの`db.batch()`へまとめる
  - [x] 開発用Neonで部位保存を意図的に失敗させ、分析本体が残らないことを確認
- [x] AIメニュー本体・種目の保存方法を再確認
  - [x] メニュー本体と全種目が同じ`db.batch()`に入っていることを確認
  - [x] 使用中の`drizzle-orm@0.45.2`で`db.batch()`が内部の`client.transaction()`を呼ぶことを確認
  - [x] `neon-http`では対話型`db.transaction(async ...)`が未対応であることを確認
  - [x] 開発用Neonで種目保存を意図的に失敗させ、AIメニュー本体が残らないことを確認
- [x] 途中失敗時に親データだけ残らないようにする
  - [x] AIメニュー保存はコード上対策済み
  - [x] トレーニング新規保存と身体分析保存に親だけ残る危険があることを特定
  - [x] トレーニング新規保存を修正
  - [x] 身体分析保存を修正

#### 二重送信点検

- [ ] 二重送信用のidempotency keyを統一
  - [x] AIチャットとAIメニューは`requestId`と`ai_request_guards`で重複処理を拒否
  - [x] 現在のAI対策は同じリクエストへHTTP 409を返す重複防止であり、前回の成功結果を返す完全なidempotencyではないと確認
  - [x] トレーニング記録は本人・日時・保存内容からバックエンドで重複判定UUIDを生成
  - [x] 身体分析は本人・日本の日付からバックエンドで1日単位の重複判定UUIDを生成
  - [x] 身体分析の同時通信は同じ日の受付UUIDを一意制約で1件だけにする
  - [x] 失敗時は受付記録を削除して同じ操作の再試行を許可
  - [x] 認証付き実通信で同じトレーニング記録の2件目がHTTP 409になることを確認
  - [ ] 成功済み再送へ前回結果を返す完全なidempotencyへ発展させるか判断

#### DB制約・インデックス点検

- [ ] 必要なDB制約とインデックスを追加
  - [x] 外部キー・cascade・プロフィール1対1・体重の日付重複防止を確認
  - [x] AIリクエストIDとAIメニュー・トレーニング記録の重複防止を確認
  - [x] AIメニュー・チャット・体重の既存インデックスを確認
  - [x] `training_sessions(user_id, performed_at)`インデックスをスキーマへ追加
  - [x] `training_exercises(session_id, display_order)`インデックスをスキーマへ追加
  - [x] `training_sets(training_exercise_id, set_number)`の一意インデックスをスキーマへ追加
  - [x] `body_analyses(user_id, status, analyzed_at)`インデックスをスキーマへ追加
  - [x] `body_analysis_areas(analysis_id)`インデックスをスキーマへ追加
  - [x] 時間・調子・表示順・セット番号・重量・回数・分析点数のDB範囲制約を追加
  - [x] `ai_request_guards.request_type`の許可値をDB制約へ追加
  - [x] `0008`を空のNeon開発用DBへ適用し、9個の範囲制約が作成されたことを確認
  - [ ] 元の本番DBへ`0008`を適用し、既存データが制約を満たすことを確認

#### マイグレーション点検

- [x] 本番と分離したNeon開発用プロジェクトでマイグレーションを検証
  - [x] `npx drizzle-kit check`が成功することを確認
  - [x] スナップショットには存在するが、作成SQLがないテーブルを特定
  - [x] `0007`で不足していた身体分析・AIメニュー・チャット関連6テーブルの作成SQLを補完
  - [x] `0007`へ`ai_generated_menus.training_session_id`と`performed_at`の追加SQLを収録
  - [x] `0008`でインデックス・一意制約・範囲制約を生成
  - [x] スキーマ13テーブルすべてに作成SQLが存在することを機械的に照合
  - [x] `npx drizzle-kit check`で履歴とスナップショットの整合性を確認
  - [x] 元の`musclepas`とは分離した`musclepas-development`プロジェクトをシンガポール・PostgreSQL 18で作成
  - [x] 空の開発環境へ`0000`〜`0008`の9本を最初から適用
  - [x] アプリ用13テーブルと`drizzle.__drizzle_migrations`が存在することを確認
  - [x] 9個の範囲制約と主要7インデックスが存在することを確認
  - [x] AIリクエストIDとトレーニングセット番号の重複がDBで拒否されることを確認
  - [x] 検証データをトランザクションの`ROLLBACK`で0件に戻したことを確認
  - [ ] 元の本番DBで既存DB用の差分適用を確認
  - [x] Neonコネクタの引数不整合を公式Neon CLIで回避して検証
  - [x] 2026-09-01に公式Neon CLI 4.13.0でプロジェクト・ブランチ状態を再確認
  - [x] `musclepas`の`production`ブランチが`archived`、データ転送量約5.74GB、リセット日時2026-10-01であることを確認
  - [x] 本番の上限回復を待つ間は、分離した開発用プロジェクトでDB検証を続ける方針に決定

#### 点検後の実装順

1. [x] トレーニング新規保存を`db.batch()`へ変更
2. [x] 身体分析保存を`db.batch()`へ変更
3. [x] トレーニング・身体分析の二重送信方針を決定してコード作成
4. [x] 不足するDB制約とインデックスをスキーマへ追加
5. [x] マイグレーション履歴を整理・生成
6. [x] 分離したNeon開発用プロジェクトで適用・二重送信をテスト
7. [x] APIと同じNeonトランザクションで途中失敗を起こし、3種類の親データが残らないことを開発用DBで確認

### 10. 入力チェックの統一

- [x] 各JSON APIをZodで検査
- [x] チャット文字数をフロントとバックエンドで統一（質問2000文字）
- [x] 日付の未来入力を検査
- [x] 種目数・セット数・配列・メモの最大値を決める
- [x] UUID形式を検査
- [x] Python・OpenAIの返却形式をさらに厳密に検査
- [x] 不正JSONがHTTP 400になる共通テストを追加

### 11. OpenAIの安全対策

- [x] プライバシー保護済み`safety_identifier`を送る
- [x] AIチャットの重大な危険入力にModerationを使用する
- [x] 医療診断をしないルールを全AI機能で統一
- [x] 怪我・鋭い痛み・しびれへの安全案内を統一
- [x] Tool最大実行回数とAI回答最大文字数を設定
- [x] モデル名を環境変数へ移動
- [x] プロンプトのテストケースを作る

### 12. Python身体分析の強化

- [ ] 同時送信で1日1回制限をすり抜けないようにする
  - [x] 本人IDと日本時間の日付から同じ受付UUIDを作る
  - [x] DB一意制約で同日の同時処理を1件にするコードを作成
  - [ ] Neon反映後に同時送信テスト
- [x] 画像3枚全体を24MB、1枚を8MBに制限
- [x] 読み込み上限を設け、容量超過画像を最後までメモリへ読み込まない
- [x] Pythonのタイムアウト・接続・APIエラーをTypeScriptのHTTPエラーへ引き継ぐ
- [x] OpenAI画像分析失敗時のテスト
  - [x] タイムアウトをHTTP 504へ変換
  - [x] 接続失敗をHTTP 503へ変換
  - [x] 利用制限をHTTP 429へ変換
  - [x] API設定エラーをHTTP 500へ変換
  - [x] OpenAI側の障害と不正な分析結果をHTTP 502へ変換
  - [x] OpenAI APIを実際に呼ばない自動テスト6件が成功
- [x] 合成画像3枚を使い、PythonからOpenAIへ実通信して決めた分析JSONを受け取る
- [x] `/health`の定期監視
  - [x] GitHub Actionsで毎時2回実行
  - [x] 公開TypeScript API・Neon・Renderをまとめて確認
  - [x] 直近の定期実行が成功していることを公開GitHub APIで確認

## 画像とプライバシー

初期版の推奨は、身体写真を長期保存せず、分析結果だけをNeonへ保存する方針です。

- [ ] 身体写真を保存しない方針を正式決定
- [ ] 一時画像を処理後に保持しないことを確認
- [ ] 参考画像を長期保存するか決める
- [ ] 保存する場合はCloudflare R2などの画像ストレージを導入
- [ ] 署名付きURL・本人だけのアクセス制御・保存期間を決める

## テストとセキュリティ

- [x] 未ログインAPIがHTTP 401を返す
  - [x] 公開APIのGET・POST・PATCH・DELETE全26通信を自動確認
- [x] 他人のデータへアクセスできない
  - [x] 開発用Neonで別ユーザーIDを条件にした取得・更新・削除が0件になることを確認
  - [x] Clerk上に一時テストユーザーを2人作り、実セッショントークンでAPIを呼ぶE2E確認
- [x] 不正入力を保存前に拒否する
  - [x] Zod入力・UUID・日付・文字数・件数・数値範囲を自動テスト
  - [x] 不正JSONの共通処理がHTTP 400を返すことを確認
- [x] 利用上限がHTTP 429を返す
  - [x] OpenAIのRateLimitErrorをHTTP 429へ変換する模擬テスト
  - [x] チャット・AIメニュー・身体分析の日次上限を認証付き実通信で確認
- [ ] Neon・OpenAI・Python障害時に安全なエラーを返す
  - [x] OpenAIのタイムアウト・接続・認証・利用制限・サーバー障害を自動テスト
  - [ ] Neon停止時とRender停止時を公開環境で確認
- [x] 2人のテストユーザーのデータが混ざらない
  - [x] 開発DB内の2ユーザー条件で取得・更新・削除の分離を自動テスト
  - [x] 2つのClerk実セッショントークンで体重・記録・AIメニューの他人操作が404になることを確認
- [ ] API単体テストとフロントからの通しテスト
  - [x] API入力・Python・DB・公開APIの自動テスト
  - [x] ExpoのAPI通信処理8件を全体テストへ追加し、TypeScript・Expo Lintとともに合格
  - [x] 開発用バイパスでトレーニング保存からカレンダー表示まで画面内通し確認
  - [x] ログイン済み画面から取得系APIの通し確認
  - [x] ログイン済み画面からトレーニング記録を保存し、再読み込み後にNeonから復元できることを確認
  - [x] 保存した記録がカレンダーへ表示されることを確認
  - [x] ログイン済みトークンから体重・記録・AIメニューの更新・削除APIを通し確認
  - [x] ホーム・最新AIメニュー・チャット履歴をログイン済み画面で取得できることを確認
  - [x] AIチャットが最新のトレーニング記録Toolを使い、重量・回数・セット・時間を回答へ反映することを確認
  - [x] AIメニュー再生成が最近の記録と過去メニューを参照し、別構成を生成・保存することを確認
  - [x] 鋭い痛み・しびれへのAI安全回答と会話保存を確認
  - [x] 分析履歴画面のGET連続実行を修正し、空状態の表示と通信停止を再確認
  - [x] 2026-09-09にローカルAPIの`/api/health`がHTTP 200を返し、`localhost:8081`向けCORSヘッダーが付くことを確認
  - [x] 2026-09-09に公開APIのヘルスチェックと未ログイン26通信の認証拒否を再確認
  - [x] 2026-09-09にNeon実DBの制約・更新・削除・cascade・2ユーザー分離テストを再確認
  - [x] RenderのPython APIからOpenAIへ合成画像3枚を送り、7部位を含む分析JSONが返ることを確認
  - [x] ブラウザ拡張に依存しないClerk実セッショントークンで更新・削除を最終確認
- [ ] iPhone実機テスト
  - [x] Expo WebをiPhone相当の390×844で確認し、主要4画面の横はみ出しが0件
  - [ ] 実際のiPhoneへExpo Goまたは開発ビルドを入れて、カメラ権限と写真選択を確認
- [x] ログへ身体情報・写真・秘密鍵を出さない
  - [x] mobileへの秘密鍵混入・`.env`除外・サーバーログへの画像/認証変数の直接出力を自動テスト
  - [x] API内の例外を本文・stackなしの安全な共通ログへ変更
  - [x] OpenAI利用ログへ質問・回答・画像・身体情報を含めない自動テスト
- [x] リクエストIDをログへ付ける
  - [x] フロントの安全な`X-Request-ID`を引き継ぎ、未指定・不正時はUUIDを作成
  - [x] APIレスポンスの`X-Request-ID`とアクセスログへ同じIDを追加
  - [x] メソッド・パス・HTTP状態・処理時間だけを構造化ログへ記録
  - [x] ローカル本番通信でHTTP 200・401のID一致を確認
  - [x] 公開Workerログ91件を点検し、OpenAI・Clerk・DATABASE_URL・Bearer・メールの露出が0件、Authorizationが伏字であることを確認
- [ ] Clerkを開発用から本番用へ切り替える
  - [x] 公開可能キーと秘密キーの組み合わせを、値を表示せず`development`・`production`・`mixed`・`missing`へ判定
  - [x] `GET /api/health`へ`authenticationMode`を追加
  - [ ] Clerk本番インスタンス作成後、`pk_live_`と`sk_live_`を公開先へ設定
- [ ] Expo依存の`npm audit`警告をSDK更新時に再確認
  - [x] 2026-09-09に本番依存を監査し、critical 0件を確認
  - [x] 現在のExpo SDK 57推奨版では自動修正版がない警告を、互換性を壊す`--force`で変更しない方針を確認

## 公開後または最後に行うこと

- [ ] OpenAIの予算アラートを管理画面で確認
- [x] 公開APIとRenderの定期ヘルス監視
  - [x] `/api/health`でTypeScript API本体とNeonの`SELECT 1`を確認
  - [x] Neon接続失敗時は秘密情報を含めずHTTP 503を返す
  - [x] `scripts/health-check.mjs`でTypeScript・Neon・RenderのHTTPとJSONを確認
  - [x] GitHub Actionsで毎時2回の定期確認を追加
  - [x] Sitesバージョン5を公開し、公開Neon接続を含むHTTP 200を確認
  - [x] GitHub Actionsが有効で直近の定期実行が成功していることを確認
- [x] Neonのバックアップ・復元確認
  - [x] 開発DBから期限付きブランチを作り、マイグレーション10件とデータ複製を確認
  - [x] 確認後に一時ブランチを削除
- [ ] 秘密鍵の定期変更
- [ ] プライバシーポリシー・利用規約
- [ ] AI利用と身体写真の利用目的・保存期間の説明
- [ ] App Store課金
- [ ] サブスクリプション状態の保存とレシート検証
- [ ] 無料回数と有料回数の切り替え

## これから進める順番

1. Chromeの通信遮断を解除し、体重履歴とトレーニング記録の更新・削除をログイン済み画面で確認する。
2. 2つの実Clerkアカウントでユーザー分離を確認する。
3. 身体分析を画面からPython・OpenAI・Neonまで通して確認する。
4. [x] 食事管理をMVPへ追加し、Neon保存APIとAI連携を作成する。
5. 食事APIを公開先へ反映し、ログイン済み画面で最終確認する。
6. App Store公開準備と課金へ進む。

## 今やらなくてよいこと

- 複数のAIエージェント構成
- 身体写真の長期保存
- ベクトルDBによる複雑な長期記憶
- 課金機能
- 大規模利用者向けの高度な分析基盤

現在のNeonにあるプロフィール・分析・記録・AIメニュー・チャット履歴とTool構成で、初期版の長期記憶は作れます。
