# 筋トレPAS フロントエンド進捗

このファイルは、Expo／React Native版の実装状況をチームで共有するための記録です。
Codexはフロントエンドを変更するたびに、このファイルも更新します。

最終更新日：2026-09-12

## 今回の対応（2026-09-12）

- 食事管理画面を仮の画面内保存から`/api/food-records`へ接続。
- 画面を開いたときに日本時間の当日データをNeonから取得し、再読み込み後も表示できるように変更。
- 食事区分・食事名・カロリー・たんぱく質をPOST保存し、本人の記録だけを確認後にDELETEできるように変更。
- 読み込み・保存・削除・エラー・成功表示と、連打による二重送信の防止を追加。
- 食事API通信テスト4件、mobile TypeScript、Expo Lintに合格。
- バックエンド側ではPATCH更新も実装済み。現在の画面には編集UIがないため、初期版では追加・一覧・削除を使用する。

## 今回の対応（2026-09-07）

- ホームへAIメニューの部位・調子選択と生成／再生成を統合し、下部ナビの重複したAIコーチ項目を整理。
- 開発用データで背中・調子5の再生成と、4種目・重量・回数・セットの記録画面への引き継ぎをWeb確認。
- 「部位・調子を変えてメニューを調整」押下時、画面上部の設定欄まで自動スクロールするよう改善。TypeScript型チェックとExpo Lintが成功。
- 下部ナビを「ホーム・トレーニング・食事管理・チャット・マイページ」の順へ変更。
- 食事管理の初期画面を追加し、食事区分・食事名・カロリー・たんぱく質の仮記録と当日合計を実装。Webで1件追加と集計反映を確認。
- 食事データの永続保存は接続済み。目標値と課金判定は今後の仕様確定後に接続する。

## 今回の対応（2026-09-06）

- 開発用APIバイパス時に認証画面へ戻されていたホーム・AIメニュー・AIチャットの画面保護を調整。
- 未ログインでも開発用ホームデータ、AIメニュー生成、AIチャットの入力・Markdown疑似回答・ローカル削除を確認可能にした。
- AIチャットの狭い画面向け余白、Markdown表の折り返し、履歴メニューの操作領域を調整。
- AIチャットと記録画面で、iOS・Androidのキーボード回避と入力・送信・保存状態の読み上げ情報を補完。
- 身体分析と体重記録にもiOS・Androidのキーボード回避を追加し、日付・体重・保存操作の読み上げ情報を補完。
- 開発用バイパス時のトレーニング記録を端末内履歴へ保存できるようにし、履歴・カレンダー反映をAPIなしで確認可能にした。
- 記録保存へ同期ロックを追加し、素早い連打による二重保存を防止。API疑似通信テスト8件も成功。
- 既存18ルートのWeb書き出しを再確認し、全ルート成功。初回設定のトレーニング形式を仕様どおり必須化し、保存連打を同期ロックで防止。
- 青テーマ移行後も残っていたカレンダーの「金色の印」を「青色の印」へ修正。TypeScript型チェックとExpo Lintも成功。
- TypeScript型チェックとExpo Lintが成功。

## 今回の対応（2026-09-05）

### 青×黒テーマへの全面変更

- 全27ファイルの主要配色を、ゴールド系から鮮やかなシアンブルーと青みのある黒へ変更。
- ボタン、選択状態、見出し、グラフ、下部ナビ、AIチャット、入力欄、カード、起動画面背景を同じパレットへ統一。
- Expo Webを390×844のスマートフォン幅で開き、ログイン画面、理想体型画面、選択済みカードの色・文字コントラスト・表示崩れがないことを確認。
- バックエンド変更は不要。次は実機表示とアプリアイコン・起動画面画像を新テーマへ合わせるか確認する。

### テスト状況の棚卸しと公開API接続準備

- `BACKEND_ROADMAP.md`と`CODE_GUIDE.md`を照合し、バックエンドで確認済みの内容とフロント未確認の内容を分離。
- 公開API URLを`mobile/.env.local`へ設定し、公開`GET /api/health`が`status: ok`・`environment: production`を返すことを確認。
- TypeScript型チェック、Expo Lint、API通信の疑似テスト8件、Expo Web全18ルートの書き出しが成功。
- 疑似テストではBearer付与、タイムアウト、取消、401、オフライン相当、画像送信を確認済み。
- 未確認：Clerkへ実ログインした画面からのホーム、初回設定、記録、AI、身体分析の通し通信と、iPhone・Android実機操作。
- バックエンド依存：Neonが利用可能な状態、テストユーザー、各APIの実データが必要。次はログイン済み端末で`bootstrap → home`を確認する。

### iOS・Androidビルドと依存関係の追加確認

- Expo SDK 57の依存関係チェックを実行し、推奨される互換バージョンへ更新。更新後は`Dependencies are up to date`を確認。
- AIチャットのMarkdown処理が参照する`punycode`不足を検出して追加し、iOS・Android・Webの全プラットフォームで書き出し成功。
- mobile専用のExpo ESLint設定を追加し、重複importと履歴Contextの不要なEffect内State更新を修正。Expo LintとTypeScript型チェックが成功。
- クライアントコードを秘密鍵名で検査し、Clerk秘密鍵・DB接続情報・OpenAI秘密鍵の混入がないことを確認。
- 公開APIテストでヘルスチェック成功と、未ログイン状態の個人データAPI26通りが401で拒否されることを確認。
- `npm audit --omit=dev`では推移的依存関係に29件のmoderate・6件のhighが残存。強制更新はExpo互換性を壊すため未実施し、Markdown依存を含め公開前に対応方針を決める。
- 次の作業：ログイン済み端末の実通信テストと、依存パッケージの脆弱性影響範囲の確認。

## 今回の対応（2026-09-01）

### 身体分析画面の分析中表示・操作補助

- 身体分析APIの処理中表示を共通ScreenStateCardへ変更し、時間がかかる場合がある旨を表示。
- 写真の撮影・選択・削除、分析開始、分析完了ボタンへ読み上げ用の役割・名前・無効/処理中状態を追加。
- 任意の体重入力へ読み上げ名を追加し、入力・権限・画像エラーをalertとして通知。入力内容と選択済み写真は失敗時も維持する既存動作を保持。
- 写真ごとの選択中表示は操作箇所に近い方が分かりやすいため、独自の小さい表示を維持。
- TypeScript型チェック、API疑似通信テスト8件、git diff --checkが成功。カメラ・写真権限、実画像送信、実機表示は未確認。
- バックエンド変更は不要。画像送信APIの実通信と分析結果JSONは公開URL設定後に確認する。
- 次の作業：bootstrapまたはチャットなど、残る独自状態表示の用途を確認して共通化する。

### AIメニュー画面の状態表示統一

- 保存済みAIメニューの取得中とAIメニュー生成中を共通ScreenStateCardへ変更。
- 保存済みメニュー取得失敗を専用エラーとして分離し、「もう一度読み込む」で取得だけを再試行できるようにした。
- 取得失敗後も今日の調子・部位を入力して新規生成でき、新規生成を始めた時点で古い取得エラーを消す。
- TypeScript型チェック、既存のAPI疑似通信テスト8件、git diff --checkが成功。Expo Webで当該状態の実表示は未確認。
- バックエンド変更は不要。実APIの成功・失敗・タイムアウト状態は公開URL設定後に確認する。
- 次の作業：身体分析画面など、独自の状態表示が残るAPI画面を用途に応じて共通化する。

## 今回の対応（2026-08-31）

### 実機確認の準備：ターミナルQR表示

- ルートの.vscode/settings.jsonに等幅フォント・文字サイズ14・行間倍率1・文字間隔0・ブロック文字描画を設定。プロジェクトをVS Codeで開いた場合だけ適用し、個人の設定・アプリUIは変更しない。
- 設定JSONの構文を確認。QRの見た目とiPhoneの読み取り成功はユーザー確認待ち。起動済みExpoプロセスは停止していない。
- 次はQR再読み取り→iPhoneで起動確認。バックエンド変更は不要。

### 通信停止時の待機上限

- 共通apiRequestにGET/HEADは30秒、その他は120秒の待機上限を追加。apiUploadRequestは120秒。timeoutMsで調整可能。
- 本文読み込みも期限に含め、期限超過時はAbortControllerで端末側の通信を中止。呼び出し側の取消signalも維持。
- 保存・生成・削除・画像送信のタイムアウトは「完了している可能性」と履歴確認を案内し、自動再送しない。サーバーの処理取消を保証するものではない。
- scripts/api-request.test.mjsの疑似通信8件が成功（正常、応答停止、本文停止、画像送信停止、401、ネットワーク失敗、送信前/途中の取消）。実トークン・外部通信なし。
- TypeScript型チェック成功。Lint・実API通信・実機の機内モードは未確認。Clerk SDK自身の通信は今回の対象外。
- バックエンド変更はなし。待機時間の妥当性と、タイムアウト後の処理結果確認・二重保存防止は実通信時に共同確認する。
- 次の作業：実機キーボード、オフライン時の画面操作、API接続後の保存→履歴取得を順に確認。

### ログイン画面のスマホ表示・入力補助

- メール・認証コード入力欄に読み上げ用の名前を追加。メール入力の自動修正を無効化。
- 認証ボタンにボタン役割・読み込み中・無効状態を設定し、エラーを読み上げ対象に変更。
- 各ボタンに余白・中央揃えを追加し、再送・メール変更の操作領域を最低48に設定。
- ブラウザスキルによる実表示確認：320×568のログイン画面、568×320でスクロールして送信ボタンを操作、未入力時のエラー表示、入力欄・ボタンのアクセシビリティ名を確認。
- TypeScript型チェック成功。今回Lintは未実施。メール送信・OTP・ログイン後画面・iPhone実機キーボード・OS文字拡大は未確認。
- バックエンド変更は不要。次はログイン済み環境のフォームとiPhone実機でキーボード表示中の操作を確認する。

- bootstrapの状態画面をスクロール可能にし、小さい画面でも下の操作へ到達できる構造に変更。
- 下部ナビの固定高さと1行制限を外し、文字拡大時にラベルを折り返せるよう変更。各タブに読み上げ用の名前を追加。
- 共通状態カードのボタンをカード幅以内に収め、長いラベルの折り返しと最低48の高さを設定。
- bootstrapの各ボタンに内側余白・中央揃え・アクセシビリティのボタン指定を追加。
- 確認：TypeScript型チェックとgit diff --check成功。Expo ESLintは終了しないため中断し、mobileのローカルeslint実行ファイルも見つからず未確認。端末上での表示・操作確認は未実施。
- バックエンド依存：今回のレイアウト修正にAPI変更は不要。実通信確認は別途API接続環境が必要。
- 次の作業：幅320程度のWeb表示と文字拡大、iPhone実機のSafe Area・キーボード表示を確認する。

今後の実装順と残作業は、バックエンドと同形式の`FRONTEND_ROADMAP.md`にも記録します。

## 実装済み

- Expo／React Native、Expo Router、Clerk認証の土台
- メール認証コードのログイン・新規登録・再送・連打防止
- タイトル画面、ログイン判定、初回設定、理想体型、身体情報、初回分析
- ホーム画面、部位選択、今日のメニューとAIメニューの連動
- トレーニング種目約100種類、検索、カテゴリ、セット入力、前回記録表示
- トレーニング記録保存・履歴取得API接続とカレンダー反映
- AIメニュー、AIチャット、マイページ、体重記録、身体分析・分析履歴
- 黒×ゴールドUI、ゴシック体、Safe Area、キーボード追従
- bootstrapの詳細状態、理想体型、プロフィール、初回設定完了、分析履歴のAPI通信
- AIメニュー生成・最新取得・Neon保存・OpenAI生成・記録画面への引き継ぎ
- 数値入力整形、入力範囲チェック、空状態、通信エラー表示
- AIメニュー再生成時の種目・順番・回数バリエーション
- 黒×ゴールドのアプリアイコン、起動画面、favicon
- Apple・GoogleのClerk OAuthログインUI
- ログアウト確認、エラー表示、ユーザー切り替え時の一時データリセット
- AIチャットの専用通信処理によるGET／POST／DELETE API接続とClerkトークン付与
- AI回答だけのMarkdown表示（見出し・太字・リスト・表・コード・改行）
- 保存済みチャット一覧、会話切り替え、新規作成、削除確認・削除中表示
- 履歴・会話・AI回答の読み込み表示、通信エラー、再読み込み・再送
- AI回答中の二重送信防止、複数行入力、2000文字制限、長文スクロール
- 公開API準備前に初回設定を確認する明示的な開発用APIバイパス
- 理想体型・身体情報・分析スキップを端末内Stateで進める開発フロー
- 本番ビルドではバイパスを強制無効化し、チーム用の`.env.example`を追加
- 身体分析の正面・横・背面写真をカメラ撮影またはライブラリから選択
- 身体写真のプレビュー・撮り直し・選び直し・個別削除・3枚の進捗表示
- 身体写真の8MB上限・画像形式チェック・選択中の連打防止
- 元画像のファイル名とMIME形式を保ったFormData送信、3枚未設定時の分析防止
- AIメニュー画面内で胸・背中・肩・腕・脚・腹筋・AIおまかせを選択
- AI画面の部位選択をホームの今日の部位へ同期し、生成APIの`requestedBodyPart`へ送信
- ホーム画面内で部位・調子を選び、AIメニューの生成／再生成からトレーニング開始まで完結するよう統合
- 重複していた下部ナビのAIコーチを外し、既存AIコーチ画面は互換用ルートとして維持
- 部位別設定では部位選択を必須化し、開発用バイパスでは選択部位のプレビューメニューを生成
- ホーム専用`GET /api/home`通信処理とClerkセッショントークンの付与
- ホーム表示を実API優先へ変更し、目標体型・最新AIメニュー・種目・推奨時間・コンディション・AIメッセージを反映
- ホームのAPI通信中・通信エラー・再試行・メニュー未生成・目標体型未設定の状態表示
- AIメニュー未生成時は仮メニューを出さず、AIメニュー生成画面へのボタンを表示
- API取得メニューをTrainingDraftContextへ変換し、トレーニング記録画面へ引き継ぐ処理
- `homePreview`は明示的な開発用APIバイパス時だけ使用
- API未公開中でもbootstrapエラー画面からホームを直接確認できる開発専用ボタン
- 読み込み中・空状態・通信エラー・再試行を統一する共通`ScreenStateCard`
- ホームとカレンダーの状態表示を共通コンポーネントへ統一
- 分析履歴の読み込み中・通信エラー・再試行・履歴なし表示を共通化
- 体重グラフと体重履歴の未記録表示をカード内用の共通表示へ統一
- bootstrapのユーザー情報取得中・接続エラー・再試行を共通`ScreenStateCard`へ統一

## 動作確認済み

- Expo Webでの主要画面表示
- Clerkのメール認証コード画面までの遷移と入力チェック
- TypeScript型チェックとExpo ESLint
- バックエンド全体のVinextビルド
- 分析履歴・初回設定完了・AIメニューAPIが未認証通信をHTTP 401で拒否
- NeonでAIメニューの保存・再取得、OpenAIによるZod形式の生成
- AIチャット競合解消後のTypeScript型チェックとExpo ESLint
- 開発用APIバイパス追加後のTypeScript型チェックとExpo ESLint
- 身体写真UI改善後のTypeScript型チェックとExpo ESLint
- AIメニュー部位選択追加後のTypeScript型チェックとExpo ESLint
- ホームAPI接続追加後のTypeScript型チェック、Expo ESLint、Git差分チェック
- bootstrapの開発用ホーム確認導線追加後のTypeScript型チェックとExpo ESLint
- 共通状態表示追加後のTypeScript型チェック、Expo ESLint、Git差分チェック
- 分析履歴・体重履歴の状態表示統一後のTypeScript型チェック、Expo ESLint、Git差分チェック
- bootstrap状態表示統一後のTypeScript型チェック、API通信テスト、Git差分チェック
- ホーム統合後のTypeScript型チェックとExpo ESLint
- 開発用Webで背中・調子5の再生成、表示更新、記録画面への4種目・重量・回数・セット引き継ぎ
- 2026-09-07に通信処理の疑似テスト8件、TypeScript型検査、Expo ESLintを再実行して合格
- Expo SDK 57公式構成に合わせ、`eslint`と`eslint-config-expo`を開発依存へ追加
- `npm run test:api`でフロント通信テストだけを再実行できるように設定
- ルートの`npm run test:all`へフロント通信テスト・Expo TypeScript・Expo Lintを組み込み、バックエンド・Python・Neon・公開APIとまとめて再確認可能にした
- Expo Webを開発用バイパスで操作し、トレーニング記録の保存成功表示と、画面内ナビゲーション後のカレンダーへの当日1件・3種目表示を確認
- テスト後は`EXPO_PUBLIC_ENABLE_API_BYPASS=false`へ戻した。次のE2EはClerkへログインした状態でAPI経由のNeon保存・再読み込み後の復元を確認する
- 2026-09-08にClerkログイン済みのExpo Webからトレーニング記録をAPI保存し、ブラウザ再読み込み後も各種目の「前回」欄へNeonの値が復元されることを確認
- 同じ記録がカレンダーへ当日1件・3種目・45分・メモ付きで表示されることを確認
- ログイン済み画面でホームの実データ、保存済みAIメニュー、保存済みAIチャットを取得できることを確認
- 身体写真が0/3枚のとき分析ボタンが無効になることを確認
- マイページで身長・体重・トレーニング形式が未入力の場合に入力エラーが表示されることを確認
- 分析履歴画面で`GET /api/body-analysis`が連続実行され、読み込み表示から進まない不具合を確認
- 分析履歴の`getToken`をRef経由で参照し、`loadHistory`を安定させてフォーカス中の連続GETを防止
- 修正後のログイン済み画面で、読み込み表示から「分析履歴はまだありません」へ進むことを確認。初回取得後に5秒待っても追加GETが発生せず、連続実行が止まったことを確認
- マイページをプロフィールAPIへ接続し、画面表示時のGET取得と保存ボタンのPATCH更新を実装
- 身長・体重・体脂肪率・場所・形式・週回数・1回の時間・苦手部位をフォームへ復元できるようにした
- `trainingStyle`をプロフィールAPIとNeonへ追加し、全身・部位別・AIおまかせが再起動後も残る構成へ変更
- 読み込み中、取得・保存エラー、二重送信防止、開発用バイパス表示を追加
- マイページを実際に開き、Neonの身長177cm・体重66kgがフォームへ表示されることを確認
- 体重履歴を`GET /api/weight-records`へ接続し、通常モードでは固定データを表示しないように変更
- 体重の新規保存をPOST、同じ日付・選択中記録の変更をPATCH、削除をDELETEへ接続
- 記録履歴へ編集・削除ボタンを追加し、編集時は日付を固定して体重だけを安全に変更する
- 読み込み中・取得失敗・保存失敗・削除失敗・再読み込み・二重送信防止を追加
- 体重API通信の疑似テスト4件を追加し、共通通信8件と合わせた12件、TypeScript、Expo Lintに合格
- 開発用Neonの一時ユーザーで体重保存・取得・更新・削除・同日重複防止を再確認
- 2026-09-09に`npm run test:all`を実行し、API安全17件、秘密情報3件、Python 12件、Expo通信12件、型検査、Expo Lint、ビルド、Neon実DB、公開APIがすべて合格
- カレンダーの保存済みトレーニングへ編集・削除ボタンを追加
- 編集時は種目・セット・時間・調子・メモを記録画面へ復元し、`PATCH /api/training-records`へ送信
- 削除時は確認後に`DELETE /api/training-records`へ送信
- 更新・削除後は履歴Contextも変更し、カレンダーへ即時反映
- トレーニングPATCH・DELETE通信2件を追加し、モバイル通信テスト合計14件、型検査、Expo Lintに合格
- 開発用一時データで「保存→カレンダー表示→編集画面へ3種目を復元→変更保存→カレンダーへ戻る」を実画面確認
- 削除ボタンの表示を確認。実データを誤って消さないため、削除確定操作は行っていない
- テスト後は`EXPO_PUBLIC_ENABLE_API_BYPASS=false`へ戻し、通常モードではNeon APIを使う状態を維持
- 食事管理は通常モードでNeon実APIへ接続済み。開発用バイパス時だけ画面確認用データを使用する
- Chromeを390×844pxへ固定し、ホーム・トレーニング・チャット・マイページ・カレンダー・身体分析で横方向のはみ出しがないことを確認
- AIチャットが本日のトレーニング記録3種目をToolで取得し、具体的な改善案を回答できることを確認。再読み込み後の会話復元も成功
- AIチャットが鋭い痛み・しびれに対して運動中止と医療相談を案内し、会話がNeonから復元されることを確認
- AIメニュー再生成が直近の胸・肩記録と過去の脚メニューを参照し、異なる脚メニューを生成。再読み込み後の復元も成功
- 身体分析画面への合成画像設定はChrome拡張機能のファイルURL権限がなく未完了。Python分析APIへの合成画像実通信は別途成功
- 2026-09-09に全自動テストを再実行し、API安全17件、秘密情報3件、Python 12件、モバイル通信14件、型検査、両Lint、ビルド、Neon実DB、公開APIがすべて合格
- ローカルAPIは`http://localhost:3000/api/health`でHTTP 200とExpo Web向けCORSを確認
- Chromeでは公開APIドメインとClerk認証ドメインの両方が`ERR_BLOCKED_BY_CLIENT`になり、アプリの通信開始前にブラウザ側で遮断されることを確認。コードやNeon停止が原因ではない
- `mobile/.env.local`は公開API URLと`EXPO_PUBLIC_ENABLE_API_BYPASS=false`へ戻し、通常接続設定を維持
- Expo SDK 57の互換性検査で見つかった`expo`・`expo-router`・`@expo/ui`・`expo-glass-effect`を推奨パッチ版へ更新
- 更新後に`expo install --check`、通信テスト14件、TypeScript型検査、Expo Lint、Webバンドルが合格
- `npm audit --omit=dev`ではcritical 0件、moderate 28件、high 7件。多くはExpo・Metro・Clerkの間接依存で現SDK内に修正版がないため、互換性を壊す`npm audit fix --force`は実行していない

## バックエンド待ち

- Neonを使う認証付きAPIが実通信できる状態
- 体重履歴APIとのフロント接続は完了。ログイン済み実画面の最終操作確認のみ残る
- 身体画像を保存する画像ストレージ
- Clerk側のApple・Googleソーシャル接続設定
- `GET /api/home`の認証付き実データ確認

## 現在接続中

- 理想体型のNeon保存（実装済み・通し確認待ち）
- 身体プロフィールのNeon保存と起動時復元（実装済み・通し確認待ち）
- 初回身体分析後のオンボーディング完了保存（実装済み・通し確認待ち）
- 分析履歴のNeon実データ表示（実装済み・通し確認待ち）
- AIメニューのOpenAI生成とNeon復元（実装済み・通し確認待ち）
- AIチャットのNeon履歴・OpenAI回答・削除（実装済み・通し確認待ち）

実際のOpenAI生成とNeon保存・取得は個別に確認済みです。ログイン中のClerkトークンを使った画面からの通し確認には、バックエンドの起動とテストユーザーでのログインが必要です。

## 2026-08-20以降の主な変更

- bootstrap後にNeonの理想体型と身体プロフィールをContextへ復元
- 身体情報入力画面を`PATCH /api/users/profile`へ接続
- 身長・体重だけを必須とし、トレーニング形式を任意入力へ変更
- 初回分析から身体写真分析へ進み、完了後に`POST /api/users/onboarding-complete`を実行
- 分析履歴画面を`GET /api/body-analysis`へ接続し、実データ・空状態・再試行を表示
- 開発用API接続先をMacのLANアドレス`192.168.68.51:3000`へ更新
- AIメニューの生成POST・最新取得GETと記録画面への安全な引き継ぎを追加
- 未ログイン時にAIメニュー・AIチャットからログイン画面へ戻す保護を追加
- 部位別トレーニングで選んだ部位をAI生成条件として送信
- バックエンド側のAIチャット通信・履歴ContextとMarkdown・再送UIを統合

未入力の重量・回数・時間・調子・メモ・bodyAreaなどは、バックエンドで`null`を許可済みです。

## 次に進める順番

1. 体重履歴とトレーニング記録の更新・削除をログイン済み実画面で最終確認（ブラウザ操作接続の復旧後）
2. 身体分析を画面からPython・OpenAI・Neonまで通し確認
3. 2つのClerkアカウントでユーザーごとのデータ分離を確認
4. 食事管理のログイン済み実画面で保存・再読み込み・削除を最終確認
7. 入力チェック、空状態、通信エラーを最終調整
8. iOS・Android・WebとiPhone実機で全画面を確認
9. TestFlightとApp Store公開準備

## 実通信確認と並行して進められること

- メールログイン画面の表示調整
- 入力チェック、空状態、エラー表示の改善
- iPhone画面、Safe Area、キーボード表示の確認
- Apple・GoogleログインUIと公開素材の準備
