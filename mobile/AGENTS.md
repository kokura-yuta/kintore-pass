# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Frontend progress

After every frontend implementation change, update `../FRONTEND_PROGRESS.md` with the completed work, verification status, backend dependencies, and next task.
