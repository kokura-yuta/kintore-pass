import { AppPlaceholderScreen } from '@/components/AppPlaceholderScreen';

export default function AiCoachScreen() {
  return <AppPlaceholderScreen eyebrow="AI COACH" title="AIメニュー" description="ユーザー情報と履歴から今日のメニューを生成する画面です。" />;
}
